if __name__ == '__main__':
    def test(self, arr1, arr2, k):
        res = 0
        resArr = []
        for num1 in arr1:
            for num2 in arr2:
                resArr.append(i + j)
        resArr.sort()
        for i in range(k):
            res += resArr[i]
        return res
